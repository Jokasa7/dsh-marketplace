# Dependencies
node_modules/

# Build output (regenerated by `npm run build`)
web/dist/

# Registry build artifacts are committed? They are generated but we DO commit
# web/public/index.json so the site works out of the box without a build step.
# If you prefer, uncomment the next line to force regeneration on clone.
# web/public/index.json
# web/public/index.json.gz
# web/public/meta.json

# Local tooling
.DS_Store
*.log
.vite/
.npmcache/
